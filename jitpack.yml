jdk:
  - openjdk17
install:
  - export JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java))))
  - ./gradlew clean build publishToMavenLocal -Pgroup=io.swaggy.swagger -Partifact=customLib -Pversion=0.0.1-SNAPSHOT